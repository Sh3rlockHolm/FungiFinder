// Copy this file to `src/runtime-config.js` and set real values before deploy.
// The app will POST feedback entries here so logs are shared across users.
window.FUNGI_FEEDBACK_ENDPOINT = "";

// Optional API key setup for your endpoint.
window.FUNGI_FEEDBACK_API_KEY = "";
window.FUNGI_FEEDBACK_API_KEY_HEADER = "x-api-key";
